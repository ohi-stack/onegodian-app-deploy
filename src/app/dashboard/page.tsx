import { appCards } from '@/data/appRegistry';
export default function DashboardPage(){return <main className="page"><a href="/">← Home</a><p className="kicker">Member Gateway</p><h1>OneGodian App Dashboard</h1><p className="lead">Open the current public and member-facing OneGodian modules. Administrative and operator controls are intentionally excluded.</p><section className="module-grid">{appCards.map(([title,description,href])=><article className="module-card" key={href}><span className="status-badge">Available</span><h2>{title}</h2><p>{description}</p><a className="button secondary" href={href}>Open</a></article>)}</section></main>}