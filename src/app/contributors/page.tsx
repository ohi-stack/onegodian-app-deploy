import { contributorNotice, contributorTiers } from '@/data/appRegistry';
export default function Page(){return <main className="page"><a href="/dashboard">← Dashboard</a><p className="kicker">Voluntary Support</p><h1>Contributors</h1><p className="lead">Contributors support ONEGODIAN, LLC public-facing products, education, media, technology, membership, and community infrastructure.</p><section className="module-grid">{contributorTiers.map(([name,amount])=><article className="module-card" key={name}><span>Contribution Tier</span><h2>{name}</h2><p className="tier-price">{amount}</p></article>)}</section><section className="notice-card"><h2>Contributor Legal Notice</h2><p>{contributorNotice}</p><p>Contribution processing is not implemented in this app. Any enabled commerce or payment flow must route to the appropriate OneGodian.com backend.</p></section></main>}