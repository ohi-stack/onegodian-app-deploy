import { NextResponse } from 'next/server'; import { appCards } from '@/data/appRegistry';
export async function GET(){return NextResponse.json({service:'onegodian-app-tools',domain:'app.onegodian.com',role:'public/member-facing',tools:appCards.map(([title,description,route])=>({id:title.toLowerCase().replace(/\s+/g,'-'),name:title,description,route,status:'available'})),excluded:['operator controls','deployment controls','kill-switches','admin runtime mutation','internal logs','privileged console functions'],timestamp:new Date().toISOString()});}